CREATE TABLE NETWORKS (ID bigint NOT NULL, NAME varchar(100) NOT NULL);
CREATE TABLE NOTIFICATIONS (ID bigint NOT NULL, TYPE varchar(100) NOT NULL,MESSAGE varchar(1000));
CREATE TABLE ROLE (ID bigint NOT NULL,NAME varchar(50) NOT NULL,TYPE varchar(50) NOT NULL);
CREATE TABLE SITE (ID bigint NOT NULL,NAME varchar(100) NOT NULL,NETWORK_ID bigint NOT NULL);
CREATE TABLE SITE_USER (ID char(100) NOT NULL,USERNAME varchar(50),EMAIL varchar(100),PASSWORD varchar(100),REAL_NAME varchar(100),STATUS int);
CREATE TABLE USER_ROLE (USER_ID char(16) NOT NULL,ROLE_ID bigint NOT NULL);
create table properties (property_id int, name varchar(256), value varchar(256));