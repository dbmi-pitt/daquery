CREATE TABLE SITE_USER (
	ID NUMBER(15) NOT NULL,
	LASTNAME VARCHAR2(255),
	FIRSTNAME VARCHAR2(255),
	LOGIN VARCHAR2(500),
	PASSWORD VARCHAR2(500),
	PRIMARY KEY (ID)
);


CREATE TABLE CHB69.INBOUND_QUERY (
	ID NUMBER(15) NOT NULL,
	STUDYNAME VARCHAR2(500),
	QUERYNAME VARCHAR2(500),
	QUERYTYPE VARCHAR2(500),
	SITE VARCHAR2(500),
	USERNAME VARCHAR2(500),
	LASTUPDATE TIMESTAMP,
	STATUS VARCHAR2(500),
	ORACLEQUERY VARCHAR2(500),
	SQLQUERY VARCHAR2(500),
	PRIMARY KEY (ID)
);

CREATE TABLE CHB69.OUTBOUND_QUERY (
	ID NUMBER(15) NOT NULL,
	STUDYNAME VARCHAR2(500),
	QUERYNAME VARCHAR2(500),
	QUERYTYPE VARCHAR2(500),
	SITE VARCHAR2(500),
	USERNAME VARCHAR2(500),
	LASTUPDATE TIMESTAMP,
	STATUS VARCHAR2(500),
	ORACLEQUERY VARCHAR2(500),
	SQLQUERY VARCHAR2(500),
	PRIMARY KEY (ID)
);



create sequence SEQ_DAQUERY
minvalue 1
maxvalue 9999999999999999999999999999
start with 1
increment by 1;